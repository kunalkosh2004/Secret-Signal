[phases.setup]
nixPkgs = ["python312", "gcc", "libpq-dev"]

[phases.install]
cmds = ["pip install --no-cache-dir ."]

[start]
cmd = "alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
